#include <stdio.h>

/**
 * main - defines the entry point for the prpogram
 *
 * Return: return 0 if errors, if not return non zero if no errors
 */
int main()
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return 0;
}
