#include <stdio.h>

/**
 * main - Serves as the entry point for the program
 *
 * Return: By default returns zero if no errors, return non-zero if errors
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return 0;
}
