#include "main.h"
#include <string.h>

/**
 * _strlen - return the length of a string
 *
 * @s: the string pointer
 *
 * Return: the length of string
 *
 */ 

void _puts(char *str);
{
        _putchar(strlen(s));
	_putchar('\n');
}

